import Image from "next/image"
import themeConfig from "../theme.config"

# Eraser

Rotating the marker to the other side activates the erase mode. You can remove any point of the line segment in its radius.

<Image
  src={`${themeConfig.prefix}/erase_video.avif`}
  width={1000}
  height={500}
/>
