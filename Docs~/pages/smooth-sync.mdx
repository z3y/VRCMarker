import Image from "next/image"
import themeConfig from "../theme.config"

# Smooth Sync

Lines smoothly appear remotely while drawing

<Image
  src={`${themeConfig.prefix}/smooth-sync.avif`}
  width={1000}
  height={500}
/>
